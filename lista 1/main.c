#include <stdio.h>
#include <stdlib.h>

int main()
{
float pi,total,k,p;
pi = 3.1416;
    printf(" informe o raio de um circulo aqui:");
    scanf("%f",&k);
    p =2*pi*k;
total=pi*(k*k);
printf("a area do circulo e : %f \n E seu perimetro e: %f", total,p );
scanf(" %f",& k);


    return 0;
}
