#include <stdio.h>
#include <stdlib.h>

int main()
{int x,y,c,a;

for(x=1;x<=6;++x){
    for(y=1;y<=6,y<=x,y!=x;++y){
            for(c=1;c<=6,c<=y,c!=y;++c){
                    for(a=1;a<=6,a<=c,a!=x,a!=y,a!=c;++a){

            }

                printf("\n %d%d%d%d",x,y,c,a);
            }
            printf("\n");
        }
    }


}


